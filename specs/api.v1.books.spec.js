var expect = require('chai').expect;
var books = require('../routes/api/v1/books');

describe("Books test suite", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it('should fail', function() {
    expect(true).to.be.true;
  });
});